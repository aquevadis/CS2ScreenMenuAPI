namespace CS2ScreenMenuAPI.Enums
{
    public enum PostSelectAction
    {
        Nothing,
        Close,
        Reset
    }
    public enum MenuType
    {
        Scrollable,
        KeyPress,
        Both
    }
}